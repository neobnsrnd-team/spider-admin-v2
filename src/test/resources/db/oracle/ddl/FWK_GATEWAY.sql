-- D_SPIDERLINK.FWK_GATEWAY definition

-- DDL generated by DBeaver
-- WARNING: It may differ from actual native database DDL

-- Drop table

-- DROP TABLE D_SPIDERLINK.FWK_GATEWAY;

CREATE TABLE D_SPIDERLINK.FWK_GATEWAY (
	GW_ID VARCHAR2(20),
	GW_NAME VARCHAR2(200),
	THREAD_COUNT NUMBER(3,0),
	GW_PROPERTIES VARCHAR2(500),
	GW_DESC VARCHAR2(1000),
	GW_APP_NAME VARCHAR2(200) DEFAULT NULL,
	IO_TYPE VARCHAR2(1) DEFAULT 'O' ,
	CONSTRAINT PK_FWK_GATEWAY PRIMARY KEY (GW_ID),
	CONSTRAINT SYS_C0011606 CHECK ("GW_ID" IS NOT NULL),
	CONSTRAINT SYS_C0011607 CHECK ("IO_TYPE" IS NOT NULL)
);
CREATE UNIQUE INDEX PK_FWK_GATEWAY ON D_SPIDERLINK.FWK_GATEWAY (GW_ID);