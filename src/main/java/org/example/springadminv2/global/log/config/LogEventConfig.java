package org.example.springadminv2.global.log.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class LogEventConfig {
    // Bean registration is handled by @Component + @ConditionalOnProperty on each adapter.
    // This config exists as a placeholder for future log-related bean customization.
}
